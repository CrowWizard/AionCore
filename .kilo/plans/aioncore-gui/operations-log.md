# 操作记录

- 2026-08-08：确认当前分支为 `dev`。
- 2026-08-08：检查 `agent-studio/`，确认其为独立 Rust/GPUI workspace，包含 GPUI、`gpui-component`、会话、聊天、编辑器、终端、设置和内部 service crates。
- 2026-08-08：检查 AionCore API 和 WebSocket 契约，确认独立 GUI 应通过 REST `/api/...` 与 `/ws` 访问服务端。
- 2026-08-08：创建 `aioncore-gui/`，复制 Agent Studio 的 Cargo 配置、Rust toolchain、源码、内部 crates、资源、主题和语言包。
- 2026-08-08：明确未复制 `.git`、CI/开发者元数据、本地 `config*.json`、构建产物和运行时数据。
- 2026-08-08：创建 `.kilo/plans/aioncore-gui/PLAN.md`，记录架构、迁移边界、阶段任务、验收标准、验证命令和风险。
- 2026-08-08：未修改原 `agent-studio/`，未修改 AionCore 后端源码，未执行 commit、push 或破坏性操作。
- 2026-08-08：完成阶段 0 基线改造：`aioncore-gui` 主包、二进制和 bundle 标识改为 `aioncore-gui` / `AionCore GUI`；窗口标题和 HTTP User-Agent 同步改为 AionCore GUI 标识。
- 2026-08-08：新增 `aioncore-gui/src/core/aioncore/config.rs`。默认 backend URL 为 `http://127.0.0.1:25808`，配置文件为平台配置目录下的 `aioncore-gui/aioncore-gui.json`，仅含 backend URL、自动拉起后端开关、data/work 目录、窗口偏好和主题偏好；不保存 Provider API Key 或本地 agent 配置。支持 `--backend-url <URL>` 覆盖，限制为带 host 的 HTTP/HTTPS URL。
- 2026-08-08：替换 `aioncore-gui/src/main.rs` 启动流程：不再读取 Agent Studio `config.json`，不再注入 Node.js 路径，不再调用 `AgentManager::initialize`，不再初始化 ACP 本地 agent 或 JSONL 消息持久化；当前只加载 GUI 配置、建立 GPUI 应用和打开窗口。保留 ACP/UI 过渡 crates，因为现有 GPUI 面板仍直接引用其类型；尚未从生产依赖图删除这些 crates。
- 2026-08-08：验证结果：新增/修改文件经 `rustfmt --edition 2024 --check` 通过。`cargo fmt --all -- --check` 未通过，原因是复制的既有 Agent Studio 文件广泛不符合当前 rustfmt 输出，未自动格式化以避免阶段 0 引入无关大规模变更。`cargo check` 已完成 Git/crates.io 依赖下载并进入编译，但被系统缺少 `pango`, `gdk-3.0`, `gdk-pixbuf-2.0` 的 pkg-config 开发文件阻塞；未到达本项目 Rust 代码检查阶段。
- 2026-08-08：阶段 1：新增 `aioncore-gui/src/core/aioncore/{client,models,events,websocket}.rs`。`CoreClient` 使用可配置的 HTTP/HTTPS backend URL，提供 10 秒超时的 JSON 请求与 `/health` 请求；健康 DTO 严格镜像已读服务端 `HealthResponse` 的 `status`、`version`、`build_time` 字段。HTTP 失败映射为安全的状态码、服务端错误码、公开错误信息和 details，不记录响应正文。
- 2026-08-08：阶段 1：WebSocket URL 从 backend URL 派生为 `/ws`，按已读 `WebSocketMessage` 信封的 `name`、`data` 字段校验并原样分派；业务层可按事件名订阅 JSON data。实现显式关闭、1 至 30 秒指数退避、WebSocket 建连后的 `/health` 校验，以及 `connected`、`disconnected`、`request_failed`、`websocket_event` 内部事件。日志只记录连接状态、HTTP 方法/路径/状态码、事件名和重连次数，不记录 prompt、消息正文或响应载荷。
- 2026-08-08：阶段 1：`AppState` 持有 `CoreClient` 和 WebSocket 连接句柄，启动时按阶段 0 配置连接；标题栏读取 Core 连接状态显示 Connected、Connecting 或 Disconnected。未修改 `agent-studio/` 或 AionCore 后端，未启动 ACP agent，未创建新的本地消息持久化路径。
- 2026-08-08：阶段 1 验证：本次修改文件通过 `rustfmt --edition 2024 --check`。`cargo fmt --all -- --check` 仍因复制的既有 Agent Studio 文件格式差异失败，未格式化这些无关文件。首次 `cargo check` 被缺少 `cairo`、`atk` pkg-config 开发文件阻塞；`cargo test --lib core::aioncore` 在依赖编译期被 rustc/LLVM `SIGILL` 阻塞，未执行测试。最终 `cargo check` 同样被缺少 `gdk-3.0`、`pango`、`gdk-pixbuf-2.0` 的 pkg-config 开发文件阻塞。尚未完成真实 `aioncore --local` 联调，受上述构建环境阻塞。
- 2026-08-08：阶段 2：新增 `aioncore-gui/src/core/aioncore/conversation_store.rs`，以 AionCore `conversation_id` 管理列表、选中详情和消息历史；REST 对接 `GET/POST /api/conversations`、`GET /api/conversations/{id}`、`GET /api/conversations/{id}/messages`、`PATCH /api/conversations/{id}`、`DELETE /api/conversations/{id}`。创建、重命名、删除后刷新列表；读取详情与消息均以已读取的 `ApiResponse` 和后端 DTO 字段为依据。
- 2026-08-08：阶段 2：在 `aioncore-gui/src/core/aioncore/models.rs` 镜像 `ApiResponse`、分页列表、`ConversationResponse`、`MessageResponse`、`MessageListResponse`、创建/更新请求和 `conversation.nameUpdated` DTO；未接入 AionCore workspace crate。
- 2026-08-08：阶段 2：`SessionManagerPanel` 改为 AionCore 会话侧栏，支持 REST 初次加载、刷新、创建、删除和打开会话；默认 workspace 布局将会话侧栏放入左侧 Dock。已移除打开会话时调用本地 ACP `resume_session` 的路径。
- 2026-08-08：阶段 2：`ConversationPanel` 历史加载改为 Store 的 detail/messages REST 请求，隐藏消息不渲染，按已验证的 `MessageType` 类型显示服务端内容；未将 REST 消息转换为 ACP `SessionUpdate`。
- 2026-08-08：阶段 2：订阅并处理 `conversation.listChanged`、`conversation.nameUpdated`。事件 payload 按 AionCore 源码验证；缺失或异常字段触发 REST 刷新，已知选中会话变更刷新详情，其余变更刷新列表。未新增消息 JSONL 事实来源，未启动本地 ACP agent。
- 2026-08-08：阶段 2 验证：阶段文件通过 `rustfmt --edition 2024 --check`。`cargo fmt --all -- --check` 未通过，原因仍是复制的既有 Agent Studio 文件格式差异，未格式化无关文件。`cargo check` 无法进入 Rust 编译检查，因环境缺少 `atk`、`pango`、`gdk-3.0` pkg-config 开发文件；相关测试未执行。未修改 `agent-studio/`、AionCore 后端源码，未执行 commit、push 或破坏性操作。
- 2026-08-08：阶段 3：读取并依据 `crates/aionui-api-types/src/conversation.rs` 的 `SendMessageRequest`、`SendMessageResponse`、`CancelConversationRequest`、`CancelConversationResponse` 和 runtime 字段实现 GUI REST DTO；发送请求仅包含 `content`、空 `files`、空 `inject_skills`、`hidden=false`，GUI 未启动 ACP agent、未写 JSONL 历史。
- 2026-08-08：阶段 3：读取 `crates/aionui-conversation/src/stream_relay.rs` 实际广播逻辑，确认 `message.stream` 的 `conversation_id`、`msg_id`、`turn_id`、`type`、`data` 字段以及 `content`、`thinking`、`tool_call`、`error`、`finish` 类型；新增 `message_reducer.rs` 使用保守 JSON 分派，不将服务端事件伪装为 ACP `SessionUpdate`。未知类型仅 debug 记录并忽略。
- 2026-08-08：阶段 3：`ConversationStore` 新增发送、取消、runtime、`message.stream`/`message.userCreated` 接入；`MessageStreamReducer` 以 `msg_id` 去重/合并 REST 乐观消息、WebSocket 事件和历史刷新，并按 `conversation_id`、`turn_id` 保持 turn 状态。`ConversationPanel` 发送/取消改走 Core API，移除该面板对本地 AgentService 发送和取消路径的调用；消息视图使用 AionCore 专用 `MessageView`，没有接入 ACP SessionUpdate。
- 2026-08-08：阶段 3 验证：修改文件通过 `rustfmt --edition 2024 --check`。在 `aioncore-gui/` 执行 `cargo fmt --all -- --check` 未通过，失败来自复制的既有 Agent Studio 文件广泛格式差异，未格式化无关文件；`cargo check` 未进入 Rust 代码检查，因系统缺少 `cairo`, `atk`, `gdk-3.0`, `gdk-pixbuf-2.0` pkg-config 开发文件；定向 `cargo test --lib core::aioncore::message_reducer` 未执行到测试，rustc/LLVM 编译依赖时 SIGILL。未执行真实 AionCore live payload 联调，故用户确认、AskUserQuestion 和附件路径仍未在本阶段实现；这些事件字段没有被猜测或强制转换。仅修改 `aioncore-gui/` 与本操作日志，未修改 `agent-studio/`，未执行 commit、push 或破坏性操作。
- 2026-08-08：阶段 4：依据 `crates/aionui-api-types/src/confirmation.rs:6-13`、`crates/aionui-common/src/types.rs:50-75`、`crates/aionui-conversation/src/routes.rs:394-472` 和 `crates/aionui-conversation/src/service.rs:3134-3248`，新增 confirmation DTO、列表加载、confirm 提交、`confirmation.remove` 收敛；confirmation 选项的 JSON `value` 原样提交，未猜测字段语义。
- 2026-08-08：阶段 4：依据 `crates/aionui-api-types/src/ask.rs:3-29`、`crates/aionui-session/src/event.rs:283-303` 和 `crates/aionui-session/src/backend/types.rs:166-187`，新增 AskUserQuestion 的专用 answer 请求。GUI 只渲染能从已证实 `questions[]` 中解析出的单题、非 multiSelect 选项；多题、multiSelect 或缺字段进入明确退化提示，不发送伪造答案。Ask decline UI 尚未接入。
- 2026-08-08：阶段 4：依据 `crates/aionui-file/src/routes.rs:575-669` 与 `crates/aionui-api-types/src/chat_file.rs:15-32`，新增 multipart `/api/fs/upload`（`file`、`file_name`、`conversation_id`）调用，返回路径包装为已证实的 `ChatFileRef::Upload` 后发送；现有文本输入中的 code selection 文件会走上传闭环。剪贴板图片尚未接入上传，因当前 GUI 只有 ACP `ImageContent`，没有已证实 AionCore 媒体请求形状。
- 2026-08-08：阶段 4：读取 detail-only `prompt_capability` 生成逻辑 `crates/aionui-conversation/src/service.rs:2040-2053`、`4883-4895`，在 GUI 展示 `None` 或 image/audio 均为 false 的退化提示，附件按文件发送。
- 2026-08-08：阶段 4 验证：阶段修改文件通过 `rustfmt --edition 2024 --check`。要求的 `cargo fmt --all -- --check` 未通过，仍为复制的既有 GUI 文件格式差异，未改动无关文件。`cargo check` 在系统库探测阶段被缺少 `cairo`、`atk`、`gdk-3.0`、`gdk-pixbuf-2.0` 阻塞；定向 `cargo test --lib core::aioncore::conversation_store` 在依赖编译期被 rustc/LLVM `SIGILL` 阻塞，未执行测试体。未启动 ACP agent、未写 JSONL 历史、未保存 Provider 密钥；未修改 `agent-studio/`、AionCore 后端或其他路径，未执行 commit/push。
- 2026-08-08：阶段 5：读取阶段 0-4 实现后复用现有 `CoreClient`、`CoreEventHub`、REST JSON 请求和 WebSocket 分派，新增 `SettingsStore` 与 `TeamStore`。设置页通过 `GET/PATCH /api/settings`、`GET /api/skills`、`GET /api/assistants` 加载 AionCore 事实源，通知开关直接提交服务端；Skill 与 Assistant 仅展示已验证的响应字段。
- 2026-08-08：阶段 5：检查 `ProviderResponse` 后确认 `GET /api/providers` 会返回解密的明文 `api_key`（`crates/aionui-api-types/src/provider.rs:145-177`）；`McpServerResponse` 的 transport 可返回 headers/env（`crates/aionui-api-types/src/mcp.rs:15-35,113-131`）。为符合 GUI 不接收、不保存 secrets 的边界，GUI 没有调用或缓存 `/api/providers`、`/api/mcp`，设置面板明确显示需由后端提供脱敏 DTO 后再接入；未伪造 Provider/MCP CRUD。
- 2026-08-08：阶段 5：新增基础 Team 面板并置入现有左侧 Dock，使用 `GET /api/teams`、`GET /api/teams/{id}`、`GET /api/teams/{id}/run-state` 加载列表、选中 Team、成员与运行态；订阅并按已读 DTO 对 `team.agentStatusChanged`、成员变更、runtime/session 状态、`team.teammateMessage`、`team.taskChanged`、`team.mailboxChanged` 收敛。字段异常时保守 REST 刷新，未创建或启动任何 ACP agent。
- 2026-08-08：阶段 5 未完成：`/api/fs` 除阶段 4 上传外的文件/编辑器操作、`/api/projects` Explorer 接入，以及 Team mailbox/activity/task 的列表渲染尚未实现，避免将原 Agent Studio 的本地 workspace/文件路径逻辑误作 AionCore 服务端契约。
- 2026-08-08：阶段 5 验证：本阶段文件 `rustfmt --edition 2024 --check --config skip_children=true` 通过。要求的 `cargo fmt --all -- --check` 仍会报告复制的既有 Agent Studio 文件广泛格式差异，未格式化无关文件。`cargo check` 在 Rust 代码检查前被缺少系统 pkg-config 开发库 `gdk-3.0`、`pango`、`atk`、`cairo` 阻塞；定向 `cargo test --lib core::aioncore::team_store` 在依赖编译期被 rustc/LLVM `SIGILL` 阻塞，新增测试体未执行。未修改 `agent-studio/`、AionCore 后端或其他路径，未执行 commit/push。
- 2026-08-08：继续阶段 5：新增 `ProjectStore` 与 `ProjectPanel` 的最小服务端数据层和左侧 Dock 外壳；读取 `GET /api/projects/{project_id}`、`POST /api/fs/list` 的已验证 DTO，未把本地绝对路径直接发送给 AionCore。由于仓库已读路由没有项目列表/创建 endpoint，GUI 不猜测 project id，面板在未选择 project id 时明确显示阻塞提示。
- 2026-08-08：继续阶段 5：扩展 `TeamStore` 加载 `GET /api/teams/{id}/activity?limit=50`，Team 面板展示已验证的 mailbox/task activity 摘要，并继续通过 `team.taskChanged`、`team.mailboxChanged` 等事件触发 REST 收敛。未实现发送、暂停、取消等 Team 状态变更操作，避免扩大基础面板范围。
- 2026-08-08：继续阶段 5 验证：本次新增/修改文件通过 `rustfmt --edition 2024 --check --config skip_children=true`。`cargo fmt --all -- --check` 仍被复制工程既有文件格式差异阻塞；`cargo check` 仍在 Rust 检查前被系统缺少 `cairo`、`atk`、`gdk-3.0`、`gdk-pixbuf-2.0`、`pango` 开发库阻塞。未修改 `agent-studio/`、AionCore 后端或其他路径，未执行 commit/push。
- 2026-08-08：审计修复：从默认 GUI 生产模块图移除了本地 ACP/Agent Studio service 路径。`AppState` 和 `ServiceRegistry` 不再持有或初始化 `AgentManager`、`PermissionStore`、`AgentService`、`MessageService`、`PersistenceService`、`AgentConfigService`；ConversationPanel 仅使用 AionCore REST/WebSocket `ConversationStore`、`MessageView`、confirmation 和附件上传。
- 2026-08-08：审计修复：默认 Dock 布局仅加载 AionCore Conversation、Conversation list、Project、Team 和 Terminal。旧 Welcome、旧 Settings、Task、CodeEditor、ACP ToolCall detail 的动作和布局恢复会安全降级或显示不可用，不会启动本地 ACP agent、读取 `config.json` 或创建 JSONL 消息历史。`agentx-agent`、`agentx-services`、`agentx-acp-ui` 已不在默认 GUI workspace members 或主 package 依赖中。
- 2026-08-08：审计修复：将 Linux 系统托盘改为显式 `tray` feature，默认构建不再依赖 GTK/ATK/GDK/Pango/Cairo；启用 `--features tray` 才编译原有托盘功能。CoreClient 改用已验证的 zed-reqwest `RequestBuilder::body` 与 `Response::text` 配合 serde JSON，修复了不可用的 `.json()` 调用、WebSocket join handle、消息 reducer 借用、GPUI event closure/ElementId/import 和 API DTO 泛型反序列化错误。
- 2026-08-08：审计修复验证：`cargo fmt --all -- --check` 通过。`RUST_FONTCONFIG_DLOPEN=1 cargo check` 通过，表明 GUI Rust 代码可通过类型检查且不依赖本机 fontconfig 开发包。常规 `cargo check` 仍需本机 fontconfig 开发文件，因为 GPUI 字体栈会用 pkg-config 探测它。`RUST_FONTCONFIG_DLOPEN=1 cargo test` 在任何项目测试开始前被 Rust 1.95/LLVM codegen `SIGILL` 阻塞，受影响 crate 包括 `fastrand`、`tracing-core`、`rustix`；未将此环境崩溃误报为测试通过。未做 AionCore live HTTP/WebSocket 联调，Ask decline/multi-select、多文件媒体附件、项目列表与 Team task/mailbox 明细仍未完成。未修改 `agent-studio/`、AionCore 后端，未执行 commit/push。
- 2026-08-09：补齐 Ask 与附件：Ask 卡支持服务端规定的 decline 请求 `{ "decline": true, "answers": [] }`，单题多选以本地选择状态聚合为同一 `AskQuestionAnswer.labels` 后提交；不支持的多题格式保持不发送。剪贴板图片会序列化到受控临时目录的 PNG，再通过已核验的 `POST /api/fs/upload` 上传为 `ChatFileRef::Upload`，发送成功、失败和移除时清理临时文件。未将图片伪装为 ACP 内容。
- 2026-08-09：补齐 Team 明细：依据 `crates/aionui-team/src/routes.rs` 和 `crates/aionui-api-types/src/team.rs` 的真实契约接入 `GET /api/teams/{id}/tasks?limit=50` 与 `GET /api/teams/{id}/mailbox?limit=50`。TeamPanel 展示任务、依赖、Mailbox 摘要和文件；当前选中 team 收到 `team.taskChanged` 或 `team.mailboxChanged` 后从 REST 重新加载以收敛状态。
- 2026-08-09：补齐项目列表：新增受认证的 `GET /api/projects`，返回不含路径的 `ProjectListItemResponse`，按用户隔离并按更新时间降序。GUI ProjectPanel 加载、选择项目并在后端项目详情标记 workspace root `available` 时才调用现有文件列表接口。相关后端测试覆盖了用户隔离、排序、DTO 路由响应；未暴露 resource URI/canonical path。
- 2026-08-09：验证：`aioncore-gui` 的 `cargo fmt --all -- --check` 与 `RUST_FONTCONFIG_DLOPEN=1 cargo check` 均通过。`cargo test -p aionui-db -p aionui-project -p aionui-api-types` 在 120 秒超时前显示 `aionui-api-types` 509 个单测通过、额外 API tests 18 个通过、response tests 6 个通过，DB tests 已在执行，未取得完整最终结果，因此不记为全量通过。Windows 请在 Windows 主机安装 Rust stable、Visual Studio Build Tools (Desktop development with C++) 后，在 `aioncore-gui/` 运行 `cargo build --release`，产物为 `target\\release\\aioncore-gui.exe`；以 `aioncore-gui.exe --backend-url http://127.0.0.1:25808` 连接本地 AionCore。未修改 `agent-studio/`，未执行 commit/push。
- 2026-08-09：用户明确批准本项目跳过全局“禁止接入 CI/CD”限制。新增 `.github/workflows/aioncore-gui-windows.yml`：在 `main`/`dev` 的 GUI 路径变更、面向 `main` 的 GUI pull request 或手动触发时，使用 `windows-latest` 和 Rust 1.95.0 执行 GUI 格式检查、`cargo build --release --bin aioncore-gui`，并保留 `aioncore-gui.exe` artifact 14 天。工作流只读仓库内容，不发布、不部署、不使用写权限。已本地读取核验 workflow 触发条件、工作目录和 artifact 路径；`git diff --check` 通过。
- 2026-08-14：按用户要求重新加载原始 Agent Studio UI 源码。来源固定为 `https://github.com/sxhxliang/agent-studio` commit `0100c3f5a7d3c1c77a631dae315f839b5d062d34`；上游主 package 与 `agentx-acp-ui` 均声明 Apache-2.0。仅参考并重写 Markdown agent message、非气泡 user message、可折叠 thinking、tool call 和 tool group 组件，没有复制或接入 `agentx-agent`、`agentx-services`、本地 ACP session 或 JSONL 持久化。
- 2026-08-14：新增 AionCore 原生消息渲染组件，直接消费已核验的 `MessageView` JSON 契约。消息 reducer 接入服务端已存在的 `acp_tool_call`、`tool_group` 和 `tips` 实时帧，对非文本工具增量执行递归字段合并，并保留 thinking done 帧的完成状态与耗时；相关测试覆盖原生 tool call、ACP tool call、增量工具更新、工程消息类型和 thinking 完成帧。
- 2026-08-14：验证通过：`cargo fmt --all -- --check`、`RUST_FONTCONFIG_DLOPEN=1 cargo check`、`RUST_FONTCONFIG_DLOPEN=1 cargo clippy -- -D warnings`。普通 `cargo test --lib` 首次在第三方 `notify-types` LLVM 优化阶段触发 Rust 1.95 `SIGSEGV`；改用 `RUSTFLAGS='-C opt-level=0' CARGO_BUILD_JOBS=1` 后完整执行 22 个库测试，全部通过。未执行真实 AionCore live WebSocket 联调或桌面截图验证，未修改用户已有的 `app_state.rs` 改动及无关 `.opencode` 文件，未执行 commit/push。
- 2026-08-15：重构 Conversation 侧栏信息架构。使用 Agent Studio CodeEditor 已采用的 `gpui_component::tree` 能力，将 AionCore Conversation 按 workspace 聚合为可展开 Tree；创建入口与浏览区分离，逐行删除按钮收敛为仅在选中 Conversation 叶节点时可用的顶部动作。Saved aionrs session 作为独立、默认折叠的只读分组，显示 summary、model 和消息数，不恢复本地 agent 生命周期能力。
- 2026-08-15：Conversation Tree 在 REST/WebSocket 数据刷新前保存稳定 ID 对应的选择和展开状态，重建后恢复仍存在的节点；删除成功后立即同步 Tree，已删除节点自然清除选择。workspace 标签加入父目录以区分常见同名目录，空状态同时考虑 Conversation 与 aionrs 两个加载源，纯图标动作增加 tooltip。测试覆盖 workspace 聚合、saved session 分离与禁用、刷新后的展开状态保留。
- 2026-08-15：验证通过：`cargo fmt --all -- --check`、`RUST_FONTCONFIG_DLOPEN=1 cargo check`、`RUST_FONTCONFIG_DLOPEN=1 cargo clippy -- -D warnings`，以及 `RUST_FONTCONFIG_DLOPEN=1 RUSTFLAGS='-C opt-level=0' CARGO_BUILD_JOBS=1 cargo test --lib`，共 24 个库测试全部通过。未执行桌面截图或真实 AionCore live 联调，未执行 commit/push。
- 2026-08-15：按用户需求调整 Conversation 侧栏：Tree 标题预览折叠换行空白但不修改正文；使用后端 `extra.is_temporary_workspace`，并以 `aionrs-temp`/`snow-temp` 名称作为旧数据兼容兜底，将临时会话按 `modified_at` 倒序归入 `Latest conversations`；新增顶部重命名对话框，调用既有 `PATCH /api/conversations/:id` 用户重命名接口。
- 2026-08-15：将多个 agent 创建按钮收敛为单行 `New conversation` 入口，在对话框中选择已启用 assistant，随后复用既有目录选择器创建会话；Conversation DTO 补齐 `assistant`、`name_source`、`created_at`、`modified_at`，非空会话的输入栏在 MCP 同级显示 backend 对应图标。GUI 验证通过：`cargo check`、`cargo clippy -- -D warnings`、26 个库测试全部通过；未执行桌面截图或 live 后端联调，未执行 commit/push。
